@import 'nib'

:local(.Skier)
  position absolute
  top 50%
  transform translateY(-50%)
  left 0
  right 0
  text-align center
  img
    &.right
      transform rotateY(180deg)
:local(.Tree)
  position absolute